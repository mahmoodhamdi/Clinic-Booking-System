# Branding Placeholder

This directory will hold logo variations, color tokens, and brand guideline
artefacts once a visual identity is commissioned.

Until then, the in-product branding (clinic logo + name + tagline) lives in
the `clinic_settings` table and is editable from `/admin/settings`.
